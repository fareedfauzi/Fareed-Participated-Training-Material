typedef struct _FILE_HEADER {
    char MAGIC[4]; //"DATA"
    int NumberOfRecords;
}FILE_HEADER, *PFILE_HEADER;

typedef struct _EMPLOYEE {
    char name[40];
    int age;
    int salary;
} EMPLOYEE, *PEMPLOYEE;
